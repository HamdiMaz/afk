import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * AFK extension entry point.
 *
 * The package scaffold intentionally registers no tools or commands yet. Future
 * functionality should be added here or imported from helper modules.
 */
export default function extension(_pi: ExtensionAPI): void {
	// Scaffold only.
}
